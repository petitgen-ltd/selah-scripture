[pytest]
# Collect the standard test_*.py files AND the app-parity validator, which is also
# runnable as a standalone script (python tests/validate_app_data.py).
testpaths = tests
python_files = test_*.py validate_*.py
python_functions = test_*
addopts = -ra
